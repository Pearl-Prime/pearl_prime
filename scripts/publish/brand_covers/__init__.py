"""Brand-generic cover system — generalizes scripts/publish/waystream_covers/
(one-brand) to every brand in the unified registry. Reuses the proven rendering
engine (templates/symbols/palette/fonts); only adds deterministic per-brand /
per-author assignment + a book_plan adapter + a market-agnostic FLUX pool gen.
"""
