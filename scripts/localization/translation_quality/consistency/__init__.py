"""Deterministic extraction/comparison tooling for corpus consistency
passes. Every module here does extraction or diffing only -- the "is this
actually consistent" judgment is Claude's (Lane 02), same rule as the rest
of this package."""
