# Test dependencies — used by core-tests, teacher-gates, pearl_news_gates, brand-guards.
# Install: pip install -r requirements-test.txt
pyyaml>=5.0
pytest>=7.0
pytest-timeout>=2.0
jsonschema>=4.0
feedparser>=6.0
Pillow>=9.0
numpy>=1.23  # validate_layer safe-zone pixel math (manga render lane); imported transitively by core pytest
ebooklib>=0.18
fastapi>=0.100.0
httpx>=0.24.0
