# get_these — contents moved

All useful content from this folder has been moved into the canonical layout:

| Original | New location |
|----------|--------------|
| `topic_engine_bindings.yaml` | **config/topic_engine_bindings.yaml** |
| `topic_skins.yaml` | **config/topic_skins.yaml** |
| `registry_grief.yaml` | **registry/registry_grief.yaml** |
| `*__*__*__CANONICAL.txt` | **atoms/\<persona\>/\<topic\>/\<engine\>/CANONICAL.txt** |

- **config/** — topic engine bindings and topic skins (vocabulary guard).
- **registry/** — grief section pack and other section registries.
- **atoms/** — story atoms by persona, topic, and engine (63 canonical files).

Duplicates with ` (1)` in the filename were skipped; the primary file was used.

Use **config/**, **registry/**, and **atoms/** at repo root for all further work.
