{
  "name": "phoenix-omega-pearl-prime-worker",
  "private": true,
  "description": "Root manifest for Cloudflare Workers Builds (pearl-prime). See docs/PEARL_PRIME_CLOUDFLARE_DEPLOYMENT.md.",
  "scripts": {
    "deploy": "wrangler deploy",
    "build:worker": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  },
  "engines": {
    "node": ">=20"
  }
}
