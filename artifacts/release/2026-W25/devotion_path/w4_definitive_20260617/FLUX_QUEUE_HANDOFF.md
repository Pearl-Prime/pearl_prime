# Covers — deferred to the Pearl Star install lane (W4 does not block on covers)

Per the W4 routing, covers ride the install-gated mass-build:
- **FLUX tranche** (burnout + courage = image-bearing genres): DEFERRED to the Pearl Star GPU
  job-queue (post-install). Cover split per `render_kdp_cover.py`: imposter_syndrome = PIL
  type-dominant; burnout/courage = FLUX. LoRAs `style_dp` / `saima_dp`.
- **PIL imposter covers** (type-dominant, FLUX-free): optional/local — not generated in this
  bounded proof (non-blocking). Reproduce locally with the type-dominant cover path when needed.
- No FLUX / Pearl Star / GPU dependency was used to produce this proof (draft manuscripts only).
