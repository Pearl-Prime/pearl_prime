# Flagship line-edit lane (Layer 3 — the real register lever)

**Authority:** `PEARL_PRIME_PERFECT_BOOKS_SPEC.md` §3.D (L1–L4),  
`docs/PEARL_PRIME_BESTSELLER_ACCEPTANCE_SCORECARD.md`

Composer retune is **banned** as the first response to catalog register gaps.
This lane earns `system working` via Pearl_Editor ONTGP on sampled chapters.

## Cadence

1. Pick 1–3 flagship cells per brand (persona × topic × engine).
2. Render with the four-piece chord (`spine` + `production` + `--exercise-journeys`).
3. Human / Pearl_Editor ONTGP on Ch1, mid (Ch5/6), last — write `ONTGP_VERDICT.md`.
4. Fix via atom rewrite / seam paragraphs / chapter opens — **not** composer topology.
5. Promote winning seams into banks; only then claim `acceptance_layer: system_working`
   on a catalog manifest row (requires `layer3_artifact` path — G-LAYER).

## Layout

```
artifacts/qa/flagship_line_edit/<YYYYMMDD>_<persona>__<topic>__<engine>/
  ONTGP_VERDICT.md          # PASS|WEAK|FAIL per Orient/Name/Turn/Give/Pull
  NOTES.md                  # optional seam notes
  RENDER_REF.txt            # path to book.txt / render dir
```

## Status

Scaffold only as of 2026-07-18 Wave-1. No cell has Layer-3 PASS yet
(see 100-book analysis: 0 / 100 `system working`).
