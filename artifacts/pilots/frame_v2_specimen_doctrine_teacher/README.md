# Frame v2 specimen — doctrine-heavy (master_sha teacher)

**Shape:** `gen_z_professionals` × `anxiety` × engine `overwhelm`, teacher `master_sha`, arc `gen_z_professionals__anxiety__overwhelm__F006.yaml`.

**Regenerate (spine mode):**

```bash
cd /path/to/phoenix_omega
PYTHONPATH=. python3 scripts/run_pipeline.py --pipeline-mode spine \
  --topic anxiety --persona gen_z_professionals --teacher master_sha \
  --arc config/source_of_truth/master_arcs/gen_z_professionals__anxiety__overwhelm__F006.yaml \
  --render-book --render-dir artifacts/pilots/frame_v2_specimen_doctrine_teacher \
  --quality-profile draft --no-update-freebie-index --no-job-check \
  --out artifacts/pilots/frame_v2_specimen_doctrine_teacher/plan.json
```

**Skim (agent):** `quality_summary.json` → `frame_governance_chapters` lists multiple `absolute_claim` (and spiritual density) violations on TEACHER_DOCTRINE-heavy body; per-chapter `stripped` / `softened` / `hard_fail` are empty and book-level `frame_stripped_sentences` / `frame_softened_sentences` are empty — matches **warn_only / preservation** where doctrine chapter policy applies. Compare with `artifacts/pilots/master_sha_system_fix_verify/quality_summary.json` for the same family. Draft profile: chapter flow reported 1/12 choppy jump (advisory under draft).
