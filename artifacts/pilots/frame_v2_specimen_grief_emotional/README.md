# Frame v2 specimen — grief / emotional-first

**Shape:** `tech_finance_burnout` × `grief` × engine `grief`, arc `tech_finance_burnout__grief__grief__F006.yaml`.

**Regenerate (spine mode):**

```bash
cd /path/to/phoenix_omega
PYTHONPATH=. python3 scripts/run_pipeline.py --pipeline-mode spine \
  --topic grief --persona tech_finance_burnout \
  --arc config/source_of_truth/master_arcs/tech_finance_burnout__grief__grief__F006.yaml \
  --render-book --render-dir artifacts/pilots/frame_v2_specimen_grief_emotional \
  --quality-profile draft --no-update-freebie-index --no-job-check \
  --out artifacts/pilots/frame_v2_specimen_grief_emotional/plan.json
```

**Skim (agent):** Same telemetry keys as other specimens; this run shows empty `frame_*` arrays — different emotional_job / contract mix did not surface strip/soften in output. Owner skim still recommended for missed absolutes in early somatic body if any.
