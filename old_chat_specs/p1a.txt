
Substance is right — all three changes landed correctly: docs/PROGRAM_STATE.md first in the read-list, PEARL_ARCHITECT_STATE.md correctly demoted to cap-registry-only, and Principle 10 captures the exact lessons (origin/main over working-tree, listings≠EPUB, follow SUPERSEDED headers). Good.

One fix before you ship it — the doc now contradicts itself, which is ironic given Principle 10. You changed the bootstrap head (added PROGRAM_STATE, annotated PEARL_ARCHITECT_STATE), but two meta-statements still claim it's untouched:

Under # Router Operating Principles v2: "The 9-line bootstrap above is the invocation; it is unchanged and remains the verbatim head" — no longer true.
Under ## Revert: "The 9-line bootstrap above is never affected" — also no longer true (and it's now 7 read-lines, not "9-line").
Fix both so the doc doesn't carry its own stale claim:

Intro line → "The bootstrap above is the invocation. v2 updated its read-list — docs/PROGRAM_STATE.md is now the first read (the SSOT), and PEARL_ARCHITECT_STATE.md is annotated cap-registry-only; the rest of the head is unchanged. These principles are what 'follow the Universal router prompt exactly' now means…"

Revert line → "Reverting this section restores the read-list to its pre-v2 form and removes Principles 0–10; the invocation's instruction lines are otherwise preserved."

And one sequencing reminder: this router edit must not go live until PR #1841 is merged — until then docs/PROGRAM_STATE.md isn't on main, so the router's new first read 404s. Confirm it's merged (or that PROGRAM_STATE.md is on main) before you start invoking this version. If #1841 is still open, hold the router swap.

With those two meta-lines corrected and #1841 merged, it's good to use — the rest of v1→v2 is intact and the new principle is the right reflex to have added.

is good?: Waystream production-gate fix plan
Situation
Branch
Has
[agent/waystream-handoff-pr2b](agent/waystream-handoff-pr2b) (current)
Waystream catalog/plan work; no register_output_strengthen.py, no spine register_gate wiring
[agent/pearl-dev-book-audit-fixes-20260621](agent/pearl-dev-book-audit-fixes-20260621)
Proven fixes in commits cc4056b259 (F6/F7/F13) + 4474753be9 (F1/F4); pilot register PASS, BUILD_EXIT=0
Best base: stay on agent/waystream-handoff-pr2b, cherry-pick those two commits (surgical, already gate-scored). Do not merge the full pearl-dev branch (large unrelated diff).
flowchart LR
  compose[compose_from_enriched_book] --> clean[clean_for_delivery]
  clean --> strengthen[strengthen_register_craft_output]
  strengthen --> flow[ensure_chapter_flow_cues]
  flow --> f7cap[cap_prescribed_action_density]
  f7cap --> f1dedupe[dedupe_register_f1_paragraphs]
  f1dedupe --> f4close[ensure_unique_chapter_closings]
  f4close --> gates[register + chapter_flow + book_pass]
Step 1 — Land proven fixes (cherry-pick)
From agent/waystream-handoff-pr2b:
git cherry-pick cc4056b259 4474753be9
Files introduced/updated (820 lines, all engine — no gate threshold edits):
New [phoenix_v4/rendering/register_output_strengthen.py](phoenix_v4/rendering/register_output_strengthen.py) — F6/F7/F13 strengthen, F1 dedupe, F4 closing rotation, arc-landing de-duplication
[scripts/run_pipeline.py](scripts/run_pipeline.py) — post-strengthen flow cues, final F7 cap, final F1/F4 pass, evaluate_register → register_gate_report.json, production hard-fail on register FAIL/WARN/HARD_FAIL
[phoenix_v4/rendering/book_renderer.py](phoenix_v4/rendering/book_renderer.py) — NO_ACTIONABLE_STEP guarantee lines
[scripts/release/build_epub.py](scripts/release/build_epub.py) — Chapter N parsing, --description / --disclosure
[tests/unit/rendering/test_register_output_strengthen.py](tests/unit/rendering/test_register_output_strengthen.py)
Resolve cherry-pick conflicts only in run_pipeline.py if Waystream branch touched spine path (keep both sides' intent; preserve the post-strengthen ordering above).
Run: PYTHONPATH=. python3 -m pytest tests/unit/rendering/test_register_output_strengthen.py tests/test_register_gate.py -q
Step 2 — Fix F2 (anxiety cell HARD_FAIL)
Residual: corporate_managers × anxiety × false_alarm — F2.B on a legitimate sentence ending in in. (e.g. …before walking in.). Not a dropped slot; missing from [_F2B_PHRASAL_VERB_ENDINGS](phoenix_v4/quality/register_gate.py).
Fix (precision, not threshold): extend _F2B_PHRASAL_VERB_ENDINGS with motion-arrival verbs already used elsewhere in the set:
("walking", "in"), ("walk", "in"), ("walks", "in"), ("walked", "in"),
Prove in [tests/test_register_gate.py](tests/test_register_gate.py) — add to test_f2b_excludes_grammatical_stranded_prepositions parametrize; keep test_f2b_still_hard_fails_genuine_dropped_slot green ("the path begins with." must still HARD_FAIL).
Do not widen the F2.B regex or lower HARD_FAIL severity.
Step 3 — Fix F7 (boundaries cells)
Residual: boundaries × shame — F7 FAIL (4 prescribed-action paragraphs in one chapter); boundaries × comparison — F7 WARN.
Root cause: authored EXERCISE/HOOK prose triggers F7 heuristic (imperative verb + timing cue substring like for/step/ten) faster than max_per_chapter=1 can clear when flow-cue lines re-introduce prescribed patterns.
Fix (composer output only):
In [register_output_strengthen.py](phoenix_v4/rendering/register_output_strengthen.py), add a final cap_prescribed_action_density(prose, max_per_chapter=0) pass in strengthen_register_craft_output after repair_f13_dwell_contract (keeps one genuine exercise per chapter from atoms; strips composer-generated surplus only).
Ensure [scripts/run_pipeline.py](scripts/run_pipeline.py) post-flow final cap stays at max_per_chapter=1 (already there on pearl-dev) — the strengthen-internal max_per_chapter=0 handles the boundaries overflow without touching F7 detector.
Prove: rebuild boundaries/shame; target register PASS or ADVISORY with F7=0 FAILs.
Step 4 — Gate-scored rebuild (pilot + generalize)
Pilot (must pass fully):
PYTHONPATH=. python3 scripts/run_pipeline.py --no-job-check --enforce-scene-gate \
  --pipeline-mode spine --quality-profile production --exercise-journeys \
  --persona corporate_managers --topic burnout \
  --arc config/source_of_truth/master_arcs/corporate_managers__burnout__overwhelm__F006.yaml \
  --render-book --render-dir /tmp/waystream_epub_001
Target: register_gate PASS (F1=F4=F2=F6=F12=0), BUILD_EXIT=0, ei_v2 ≥ 0.55, book_pass PASS.
EPUB (same metadata as before):
python3 scripts/release/build_epub.py \
  --input /tmp/waystream_epub_001/book.txt \
  --title "After the Flame: When Everything Is Too Much" \
  --subtitle "A Real Talk Guide to Burnout Recovery for Managers" \
  --author "Theo Castellan" --publisher "Waystream Sanctuary" \
  --cover brand-wizard-app/public/assets/covers/way_stream_sanctuary/way_stream_sanctuary__default_teacher__corporate_managers__burnout__overwhelm.png \
  --disclosure "Written for emotional clarity: secular, somatic language for managers navigating burnout — not medical advice; seek professional care when you need it." \
  --output /tmp/waystream_epub_001/way_stream_sanctuary__corporate_managers__burnout__overwhelm.epub
Generalize (register-scored only; report honestly if chapter_flow/book_quality still fail):
Cell
Arc
anxiety × false_alarm
corporate_managers__anxiety__false_alarm__F006.yaml
boundaries × shame
corporate_managers__boundaries__shame__F006.yaml
boundaries × comparison
corporate_managers__boundaries__comparison__F006.yaml
Report N/N register-PASS. Name any new F-class (e.g. chapter_flow, book_quality) without faking pass.
Step 5 — Commit (PR-not-merge)
Single plumbing commit on agent/waystream-handoff-pr2b (or agent/waystream-production-gate-fix branched from it):
fix(waystream): cherry-pick register composer passes + F2/F7 precision fixes
Constraints honored:
0 register threshold/severity edits
0 new prose (deterministic dedup/rotation/phrasal-verb precision only)
No paid API
Out of scope (honest residuals)
If cells still fail BUILD_EXIT after register PASS:
chapter_flow — separate fix in [ensure_chapter_flow_cues](phoenix_v4/rendering/book_renderer.py) / exercise journey scoring
book_quality_gate — repeated-phrase caps in [book_quality_gate](phoenix_v4/quality/book_quality_gate.py)
These are real blockers but distinct from the F1/F4/F2/F7 register work; report them in CLOSEOUT without loosening gates.

Strong, disciplined plan — it follows fix-the-defect-never-loosen-the-gate correctly (F2 = phrasal-verb precision, not regex-widen or severity-drop; F7 = composer-output cap, not detector edit; cherry-pick surgical, not full-branch merge). Let me verify its foundation + check the one scope risk before the verdict:

Ran
Verify the plan's proven-commit foundation + branch scope
Two findings — one good, one is a real crack in the foundation:

✅ cc4056b259 (F6/F7/F13) is on origin + register_output_strengthen.py is on the pearl-dev branch. And waystream-handoff-pr2b is clean (0 behind main, 1 ahead) — no drift, good base.

⚠️ 4474753be9 (the F1/F4 fix — the commit the entire "pilot register PASS, BUILD_EXIT=0" premise rests on) is NOT on any origin branch. That's the same stranded-commit pattern that's bitten us 3× this session. Let me confirm whether it even still exists locally:

Ran
Determine if the F1/F4 commit is recoverable or lost
Definitive now: 4474753be9 exists locally (it's the real F1/F4 fix on your local pearl-dev branch, "clear register F1/F4 repetition on Waystream spine renders") — but it is not pushed to origin (origin's tip is the auto-backup commit 3e3af91f24; the clean F1/F4 commit isn't in origin's history). So the "pilot passes" premise is real and reproducible in this repo, but the commit it rests on is one git gc/clone away from gone — the exact stranding pattern that's bitten us repeatedly.

Verdict: good plan, sound discipline — but do this FIRST, before Step 1:

Secure 4474753be9 to origin. It's local-only. The plan's git cherry-pick cc4056b259 4474753be9 will work in this repo (both objects are in the local DB), but if that commit isn't pushed, the whole F1/F4 fix + the gate-passing pilot can vanish. Push it now — and the cleanest way also fixes a scope issue:

Cherry-pick cc4056b259 + 4474753be9 onto a FRESH branch from origin/main (not onto the Waystream catalog branch) → push → that's a durable, reviewable, engine-only PR. These fixes (register_output_strengthen.py, book_renderer.py, run_pipeline.py) are generic — they benefit all 26 brands' future EPUBs, not just Waystream. Coupling them to the Waystream branch buries a generic engine improvement inside a catalog PR. Land them as their own PR; let Waystream (and everyone) build on it.

That single change both (a) secures the stranded commit to origin, and (b) decouples the generic engine fix from Waystream catalog work.

The rest is correct and I'd keep it:

F2 = phrasal-verb-set precision (motion-arrival verbs) + keep the dropped-slot HARD_FAIL test green — right (detector precision, not regex-widen/severity-drop). ✓
F7 = composer-output cap in the strengthen pass, detector untouched — right. ✓ One thing to verify there: how does cap_prescribed_action_density(max_per_chapter=0) distinguish "composer-generated surplus" from the "genuine atom exercise" it must preserve? If it can't cleanly tell them apart, =0 strips real exercises → ei_v2 / exercise-journey damage. Add a post-build assert: exactly 1 genuine exercise survives per chapter (not 0, not 2+).
Cherry-picking just the 2 clean commits (skipping the auto-backup noise interleaved on the branch) is the right call. ✓
Branch base is clean (waystream-handoff-pr2b is 0 behind main) — good either way. ✓
Gate-scored rebuild + honest residuals (chapter_flow/book_quality out-of-scope, named not faked) — correct. ✓
So: secure 4474753be9 to origin (ideally as a clean engine PR off main), verify the F7 cap preserves the genuine exercise, and the plan is good to execute. Without securing that commit first, Step 1 is built on a commit that only exists on one machine.

look good?: Act as my Phoenix Omega agent prompt router.
Before generating any prompt, read:
- docs/PROGRAM_STATE.md          ← current verified state (SSOT) — ground here FIRST
- docs/SESSION_UNITY_PROTOCOL.md
- docs/PEARL_ARCHITECT_STATE.md  (architecture CAP REGISTRY — cap-status only, NOT program state)
- artifacts/coordination/ACTIVE_PROJECTS.tsv
- artifacts/coordination/ACTIVE_WORKSTREAMS.tsv
- artifacts/coordination/SUBSYSTEM_AUTHORITY_MAP.tsv
Follow the "Universal router prompt (prompt-building chats)" section in docs/SESSION_UNITY_PROTOCOL.md exactly.
When I give you a plain-English task, do not solve it. Return only the final paste-ready startup prompt for the next agent chat.

---

# Router Operating Principles v2

The bootstrap above is the invocation. v2 updated its read-list — docs/PROGRAM_STATE.md is now the first read (the SSOT), and PEARL_ARCHITECT_STATE.md is annotated cap-registry-only; the rest of the head is unchanged. These principles are what "follow the Universal router prompt exactly" now means — patterns proven across live multi-cascade operation. Each is revertable via git checkout if it ever misfires.

## 0. The router's job
Return ONE paste-ready startup prompt for the next agent chat. Never solve the task. Never execute. The router authors; agents act. This isolation is what keeps the router safe to run alongside any number of live sessions.

## 1. Deconflict before authoring — the most important reflex
Before writing any prompt, scan what is already in flight:
- gh pr list --state open — open PRs + mergeable state
- artifacts/coordination/*_tracker.md — active cascades
- any held / running agents the operator mentions
Then bake collision guards into every prompt produced:
- PRE-REQUISITE CHECK at STARTUP — STOP + surface if any upstream gate is unmet. Never dispatch into a void.
- ONE git-ops driver at a time on shared coordination files: PEARL_ARCHITECT_STATE.md, ACTIVE_WORKSTREAMS.tsv, operator_decisions_log.tsv, the atom SSOT + gap matrix. Nearly every cascade writes these; two merge cascades on them simultaneously = conflict hell. Sequence, never parallelize, merges on these files.
- Sibling-PR search before any new PR: gh pr list --search "<exact title>" --state all.
- One actor per resource (PR branch / server / ratification). If a second session is about to touch a resource another is already resolving, tell it to STAND DOWN — never run two resolvers, two installs, or two ratifications on the same resource.

## 2. Default-decision posture — the operator is not a blocker
The operator QAs results, not PRs. Every prompt instructs its agent to: decide autonomously per recommended defaults → log in-envelope sub-decisions to operator_decisions_log.tsv → ship → report decisions in CLOSEOUT with rationale + one alternative considered. Escalate ONLY operator-tier business calls (legal / financial / brand identity / cross-program scope / genuinely ambiguous trade-off with no clear default). When escalating, use the walkthrough format every time: the choice → concrete example A vs B → what it means for the goal → recommendation → why.

## 3. Batch operator decisions
Surface open questions as Q-<TAG>-NN with a recommended default each, so the operator ratifies the whole set with one line ("go with defaults" / "defaults except Q-X = (b)"). Ratification is its own small PR: OPD-log rows + RESOLVED stamps in source + any cap-status PROPOSAL→ACTIVE flip. Support Mode-A (defaults) and Mode-B (per-Q) parsing.

## 4. Worktree / disk discipline
Any prompt creating worktrees must include: disk precheck (≥ 20 GB free; the .claude/worktrees/ mega-tree + ~3.2 GB/worktree LFS smudge has caused 99%-disk dispatch-kills), prune .claude/worktrees/ before fan-out, reuse an existing PR worktree over git worktree add where possible, and surgical staging ONLY — never git add -A / git add . in the mega-tree (stages 50k+ files and kills the agent). Stage explicit paths.

## 5. CLOSEOUT discipline
Full commit SHA (not short); evidence paths; the decisions-made-autonomously summary; NEXT_ACTION specific enough for a cold-start agent to resume. Drift-recovery and scale-up prompts additionally state what failed before and the known-good anchor.

## 6. Git-first drift recovery
When the operator says "this used to work" or "you keep dropping X": restore from the known-good SHA (git checkout <sha> -- <path>), lead with the audit, frame as "Nth attempt — here is what failed before, here is the anchor." Never fresh-author what git already shipped. Check the known-good anchors registry first.

## 7. Auto-open + verify
open <path> is the last step whenever operator review is needed (deck, books, evidence dir). Verify observable outputs before a CLOSEOUT claims "done"; dispatch or keystroke alone is not proof.

## 8. Standard prompt skeleton (reaffirmed)
Every prompt carries: PRE-REQUISITE CHECK (if gated) · READ FIRST · MISSION · DELIVERABLES · OPEN OPERATOR QUESTIONS (recommend defaults; do not decide) · DO NOT · GOTCHAS · GOLDEN BRANCH + PR · CLOSEOUT_RECEIPT · and a mandated DISCOVERY REPORT before the agent acts. Match heft to task: terse for quick checks, hostile-stakes framing ("Nth attempt; here's what failed") for repeated-drift recovery, full multi-section for spec / cascade work.

## 9. Reuse before authoring
Before any prompt that creates a NEW file (engine, generator, config, spec, doc), bake in a reuse-first check: edit the canonical artifact that already owns the capability rather than greenfielding a parallel one. When present, consult artifacts/coordination/CANONICAL_ARTIFACTS_REGISTRY.tsv (concept_key → canonical_path → owner) as the lookup; it promotes the highest-confidence facts from the known-good anchors registry and SUBSYSTEM_AUTHORITY_MAP.tsv. A registry row marked edit_not_recreate=YES is a singleton — edit it in place; NO-without-ratification means a new variant needs a cap-entry or operator ratification first. A genuinely new artifact is fine, but the prompt must carry the override tag NEW-ARTIFACT-JUSTIFIED: <reason> and add its own registry row in the same PR (the reinvention guard in scripts/ci/pr_governance_review.py WARNs otherwise). This is the reflex Layer 1 of the anti-reinvention system; the registry (Layer 2) and the PR guard (Layer 3) back it up. See docs/specs/CANONICAL_ARTIFACTS_REGISTRY_SPEC.md.

## 10. Ground on the SSOT + origin/main — never the working tree
Read docs/PROGRAM_STATE.md (its "LAST VERIFIED @ origin/main <sha>") as the first ground-truth before authoring. State claims resolve against origin/main, NEVER the working tree (shared-tree branch-churn shows other-branch/stale state — it showed 800-distinct titles locally while main had 186) and NEVER git-date. A "listing" (catalog metadata) is NOT a "book/EPUB" (a readable file) — never conflate them in a prompt's premise. If a doc carries a ⛔ SUPERSEDED header, follow it to the SSOT; do not author off the stale doc.

## Revert
Reverting this section restores the read-list to its pre-v2 form and removes Principles 0–10; the invocation's instruction lines are otherwise preserved.

Both contradictions are fixed cleanly — the intro now accurately says v2 updated the read-list (no more "9-line / unchanged" claim), and the Revert line correctly describes what reverting does (no more "bootstrap never affected"). The doc is internally consistent now, and Principle 1 still correctly lists PEARL_ARCHITECT_STATE.md as a hot coordination file (its cap-registry role and its write-contention status are different things — no conflict).

One live precondition to confirm before you actually invoke this version — let me check it:

Ran
Confirm PROGRAM_STATE.md is on main before the router relies on it
Yes — good, and it's now live-ready. Both fixes are clean and the gating precondition is met:

✅ Both self-contradictions resolved — the intro accurately describes the v2 read-list change; the Revert line is honest. Doc is internally consistent.
✅ PR #1841 is MERGED (21:00 UTC today) → docs/PROGRAM_STATE.md is on origin/main, so the router's new first read resolves (no 404).
✅ PEARL_ARCHITECT_STATE.md still on main → the router's cap-registry read resolves too.
So the precondition I flagged last turn is satisfied — you can invoke this version now. Nothing left to fix: the read-list grounds on the SSOT first, PEARL_ARCHITECT_STATE is correctly scoped to cap-status, Principle 10 encodes the exact failure modes that burned this session (working-tree vs origin/main, listings ≠ EPUBs, follow SUPERSEDED headers), and the rest of v1→v2 is intact.

Ship it.

is book assembly 100% deterministic? or does it include LLM. it should be determinsitic. 

but look what one agaent is seeing:[“missing cells” vs “total exercise inventory” vs “insufficient pool depth for 12-chapter plans”]. give prompt to figure out what's needed for waystream to get to 800 books ,  and for all brands can write 800 books for all topics. verify that we use the 300+ exercises and the 3 layer stories. check git history, this was working before. don't drift.

Two real questions here — (1) is assembly deterministic, and (2) the agent is conflating three different blockers. Let me verify both against the code so the prompt is grounded, not asserted:

Ran
Ground determinism + exercise/story wiring + coverage tools
Two things first, both grounded:

Is book assembly deterministic? Designed yes — with one thing to verify. The spine/production build composes pre-authored atoms (CANONICAL.txt + SOURCE_OF_TRUTH/story_atoms/ + SOURCE_OF_TRUTH/exercises_v4/) by seed + arc — no LLM at build. The LLM (Pearl_Writer) was only used upstream to author the atoms/plans, once. BUT there's a phoenix_v4/rendering/pearl_writer_expand.py in the render path that can call an LLM — and it likely connects to your agent's "insufficient pool depth." The correct deterministic design is: thin pool → raise InsufficientVariantsError (which lives in enrichment_select.py) → fix by adding atoms. The drift to catch: thin pool → silently pearl_writer_expand (LLM at build) → non-deterministic. That's the one place to verify.

The agent's three "blockers" are three different axes, not one (this conflation is the confusion):

Missing cells = (persona, topic, engine) with no master_arc → can't build at all. Tool: buildable_matrix.tsv.
Exercise inventory (311) = exercises_v4/ — global, shared across all books, not per-cell. It does not gate a single book. Tool: audit_exercise_coverage.py.
Insufficient pool depth = per-(persona, topic, slot) atom-variant count below the 12-chapter floor (e.g. REFLECTION needs ≥3). Tool: atom_coverage_audit.py — not the false-zero gap-matrix TSV.
Here's the diagnostic prompt (read-only, Sonnet — cheap, no Opus):

[Run on SONNET — read-only diagnostic + git-history; no Opus needed.]
ROLE: Pearl_Dev (book-pipeline auditor), in /Users/ahjan/phoenix_omega. READ-ONLY — no edits, no mutating builds.
MISSION: state precisely what's needed to (a) get Waystream to 800 buildable books and (b) let ALL brands build 800
books across all topics — by DISENTANGLING the three conflated metrics, VERIFYING assembly is deterministic + uses
the 311 exercises + the 3-layer story system, and CHECKING GIT HISTORY for the known-good state (this worked before —
find the anchor, do NOT fresh-fix/drift). Verify by real tool output, never assertion. Ground on docs/PROGRAM_STATE.md
(SSOT) + origin/main first.
PART A — DETERMINISM (operator's hard rule: assembly MUST be deterministic, no LLM at build):
- Trace scripts/run_pipeline.py --pipeline-mode spine --quality-profile production: confirm it composes from atoms
  (CANONICAL.txt, SOURCE_OF_TRUTH/story_atoms, SOURCE_OF_TRUTH/exercises_v4) by seed+arc, NO LLM at build.
- ⚠ phoenix_v4/rendering/pearl_writer_expand.py can call an LLM. Determine if it is INVOKED in spine/production —
  specifically, when pool depth is insufficient for 12 chapters, does the pipeline RAISE InsufficientVariantsError
  (enrichment_select.py — deterministic, correct) or FALL BACK to pearl_writer_expand (LLM at build — non-deterministic,
  the drift to flag)? Report which fires, with the call path. Correct design = raise → fix by adding atoms.
PART B — VERIFY 311 EXERCISES + 3-LAYER STORY ARE WIRED + USED:
- Exercises: confirm the build LOADS + selects from SOURCE_OF_TRUTH/exercises_v4/ (the 300+ library), NOT a hardcoded
  fallback / the mislabeled "library_34" stub. Run scripts/analysis/audit_exercise_coverage.py.
- 3-layer story: SOURCE_OF_TRUTH/story_atoms/{character_roster*.yaml, scene_injection_map.yaml,
  story_atom_tier_templates.yaml} + phoenix_v4/planning/story_planner.py (build_story_schedule). Confirm named-character
  story atoms inject at the scheduled sections (the 3 tiers / sec 2/5/9), producing named SCENE prose — NOT generic
  SCENE v04/v18. ⚠ This REGRESSED before: read docs/diagnostics/OPD-142_STORY_SCHEDULE_REGRESSION_FORENSIC_2026-05-21.md
  and memory project_known_good_anchors (PR #669 wired STORY at sec 2/5/9; PR #645 = character roster). If the current
  build has drifted from that anchor, NAME the SHA to restore from — do not fresh-author.
PART C — DISENTANGLE THE 3 METRICS (measure each with its OWN tool, report per brand):
1. MISSING CELLS = (persona,topic,engine) with no master_arc. Tool: buildable_matrix.tsv / build_buildable_matrix.py.
2. EXERCISE INVENTORY = the 311 in exercises_v4, GLOBAL/shared (clarify it does NOT gate per-book). Tool:
   audit_exercise_coverage.py.
3. PER-CELL POOL DEPTH (12-ch floor) = per-(persona,topic,slot) variant count vs need. Tool: atom_coverage_audit.py
   — NEVER the false-zero gap-matrix TSV (memory project_atom_coverage_real_number).
PART D — WHAT'S NEEDED (the deliverable):
- WAYSTREAM → 800: it's 450 composite cells (corporate_managers × topics × engines) → 800 w/ standard+1hr. Report:
  arcs present/missing; which cells fall below the 12-ch per-slot floor (+ how many variants short, which slot); the
  production-gate residual (F-classes). Concrete: X buildable now, Y need arcs, Z need N variants in slot S.
- ALL BRANDS → 800/all-topics: the full buildable matrix (834 arc-backed/locale × brands). Per brand: arc-backed vs
  missing-arc cells (thin brands need arc-seeding), per-slot depth gaps. Concrete arc-authoring + atom-variant-authoring
  needed to reach 800-all-topics.
- SPLIT the answer into: (i) DETERMINISTIC-BUILD gap = content (more atoms/arcs) vs (ii) PRODUCTION-GATE gap = the
  F-class engine work. Different lanes; don't merge them.
CONSTRAINTS: READ-ONLY (diagnostic, no edits/mutations). SONNET. Use the RIGHT tools (atom_coverage_audit,
audit_exercise_coverage, buildable_matrix) — NEVER the gap-matrix TSV or "library_34" log (both known false signals).
Git-first: this worked before — report drift + the anchor SHA, don't fresh-fix.
OUTPUT (open it): (1) determinism verdict + whether pearl_writer_expand is on the production path; (2) exercises +
3-layer-story = wired-or-drifted (+ anchor SHA if drifted); (3) the 3-axis matrix per brand; (4) concrete "what's
needed" for Waystream-800 and all-brands-800, split content-gap vs production-gate-gap. End with a CLOSEOUT_RECEIPT.
The headline this will surface: your agent's "missing cells / exercise inventory / pool depth" aren't competing answers — they're three separate gaps that need three separate fixes (author arcs / nothing-exercises-are-fine / author atom variants), and none of them should be solved by LLM-at-build — if pearl_writer_expand is firing on thin pools, that's the non-determinism to kill, and the fix is more atoms, not more LLM.

docuemtn session and update handoff .md

Let me ground the current state since the last handoff (main moved with #1841), then write it.

Running
Ground current state for the handoff update
running




