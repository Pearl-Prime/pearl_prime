# Adapting Master Wu’s Frameworks for Youth

- **Teacher**: `master_wu`
- **Purpose**: Source material for KB build + gap-fill (Teacher Mode).
- **Canonical raw**: `Adapting Master Wu's Frameworks for Youth.txt` (stored alongside this file in `raw/`).

This `.md` is intentionally a short index/synopsis. The full text is kept verbatim in the `.txt` to avoid accidental truncation or formatting drift during conversion.

