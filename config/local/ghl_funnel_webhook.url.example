# Local GHL inbound webhook URL (one line). Gitignored — operator only.
# Create from GHL: Automation → Inbound Webhook → copy URL.
# Then: python3 scripts/freebies/inject_ghl_webhook.py --require-env
