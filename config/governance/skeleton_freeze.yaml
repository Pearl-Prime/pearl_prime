# Catalog skeleton batch merge freeze (Q-DRIFT-01 / OPD-20260630-001).
# Enforced by scripts/ci/pr_governance_review.py → check_skeleton_freeze().
# Lift: set active: false when the first real R2 EPUB assembly batch ships
# (or operator_override: true for emergency unblock — requires new OPD row).
#
# Matcher: PR title must match feat(catalog): {locale} skeletons {brand} batch {N}.
# CJK locales (ja_*/zh_*/ko_*) are excluded — manga/CJK lanes stay unblocked.
# Known limitation: title-only — a renamed or differently formatted PR bypasses the gate
# (acceptable; matches the observed Jun 27–28 storm pattern).

active: false
lift_condition: "first R2 EPUB batch shipped"
opd: OPD-20260630-001
operator_override: false
# LIFTED 2026-07-01 (OPD-20260630-001 lift condition met). First real R2 EPUB
# batch shipped + live URL verified by curl (HTTP 200, byte-identical sha256).
# Cell: warrior_calm × corporate_managers × burnout × overwhelm (TUPLE_VIABLE, no
# flip needed). EPUB: register-PASS, leakage-clean, 9068 words, cover embedded.
# R2 object (private bucket, s3v4 presigned, ≤7d per R2 max-expiry cap):
#   s3://phoenix-omega-artifacts/brand/warrior_calm/deliveries/2026-W27/amazon_kdp/
#     warrior_calm__corporate_managers__burnout__overwhelm.epub
#   sha256: 2e4a6f4a7921526d0a538fdd9b35db7f5f5d953f17d7571a7adb89c2a6f70147
# Attached: brand-wizard-app/public/brand_deliveries/warrior_calm.json (2026-W27,
#   sku audiobook_en_US_warrior_calm_burnout_corporate_managers_master_wu).
# Evidence: artifacts/qa/flip_assemble_pilot_20260701/
lifted_at: "2026-07-01"
lifted_by: "Pearl_Dev+Pearl_Int (flip-assemble-pilot)"
lift_evidence_r2_key: "brand/warrior_calm/deliveries/2026-W27/amazon_kdp/warrior_calm__corporate_managers__burnout__overwhelm.epub"
lift_evidence_sha256: "2e4a6f4a7921526d0a538fdd9b35db7f5f5d953f17d7571a7adb89c2a6f70147"

# Locale prefixes excluded from the freeze (CJK book lanes — not Western listing batches).
excluded_cjk_locale_prefixes:
  - ja_
  - zh_
  - ko_

# Pre-staged spine PRs (content/CJK/lean-CI) — never blocked by the skeleton matcher.
exempt_pr_numbers:
  - 3110
  - 3123
  - 3127
  - 3147
  - 3166
