# OPD-116/117 Phase B — planner-template fallbacks when pilot atoms are not authored.
# Pearl_Editor commissions per-angle atoms under atoms/{persona}/{topic}/ANGLE_*.

generic:
  angle_definition: |
    [Angle journey — definition placeholder for {display_name}.]
    This book follows the {analogy_lens} lens: {core_frame}
    The named object for this topic will be named explicitly once commissioned.
    For now, treat the angle as the spine that every chapter returns to.
  angle_callback: |
    [Angle journey — layer {layer} callback placeholder.]
    Phase: {phase}. Prior layer: {prior_assertion}
    This chapter deepens the angle without breaking the thread established in chapter one.
