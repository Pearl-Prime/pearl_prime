# OPD-116/117 Phase B — planner-template fallbacks when pilot atoms are not authored.
# Pearl_Editor commissions per-angle atoms under atoms/{persona}/{topic}/ANGLE_*.

generic:
  angle_definition: |
    This book uses {display_name} as a steady thread for the reader.
    The central frame is simple: {core_frame}.
    Each chapter returns to that frame from a new, practical angle.
  angle_callback: |
    This chapter returns to the book's earlier thread and deepens it.
    What began as {prior_assertion} now becomes a more practical next step.
